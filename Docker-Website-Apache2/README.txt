1. Build image --> "docker build -t apache-docker-demo ."

2. Run container --> "docker run -d --name httpd-docker-01 -p 80:80 apache-docker-demo"

3. Open a browser and input "http://localhost:80/" in the address field.
   The web page should now run.
   
6. You should still se the web page.


